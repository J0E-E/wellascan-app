import { StyleSheet } from 'react-native'

const globalStyles = StyleSheet.create({
	wellaLogo: {
		height: 178,
		width: '100%',
		bottom: 0,
		left: 0,
		position: 'absolute',
	},
})

export default globalStyles